import { Metadata } from 'next';

export const metadata: Metadata = {
  title: '平行路由',
};

export default function ParallelRoutesPage() {
  return (
    <div>
      ParallelRoutesPage
    </div>
  );
}
